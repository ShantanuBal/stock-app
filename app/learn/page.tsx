import LearnContent from "./LearnContent";

export const metadata = {
  title: "Horizon University",
  description: "Financial education hub — how global markets work, from equities and bonds to currencies, geopolitics, and the US dollar as reserve currency.",
};

export default function LearnPage() {
  return <LearnContent />;
}
